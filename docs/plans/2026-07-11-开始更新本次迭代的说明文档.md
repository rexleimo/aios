# 开始更新本次迭代的说明文档

> AIOS Planning Contract (schema v2)
> created: 2026-07-11T01:10:23.479Z
> client: codex
> source: always-on-planning
> route: implement

## Objective

补齐 v3.6.0 Headroom + Ponytail token intelligence 工作流的用户说明、博客和发布记录；所有公开表述以已合入的 init/MCP 实现为准。

## Scope

- 更新 `docs-site/{,zh/}token-compression.md`，用 RTK、Caveman、Headroom MCP、ContextDB 与 Ponytail-inspired gate 的职责边界替换旧的原生拦截叙述。
- 新增 `blog-site/{,zh/}2026-07-headroom-token-intelligence.md`，并把文章接入博客首页和 MkDocs 导航。
- 扩展根 `CHANGELOG.md` 和站点的中英文 changelog，说明独立安装授权、MCP 注册、所有权 ledger、Hermes TTY 门禁及 MCP-only 限制。
- 不修改忽略的 `site/` 生成产物；以 MkDocs 严格构建和站点同步检查验证源文档。

## Route skills

1. `using-superpowers`
2. `writing-plans`
3. `test-driven-development`
4. `verification-before-completion`

## Tasks

- [x] **t1-understand**: Clarify objective: 开始更新本次迭代的说明文档 — _公开文档、博客和 changelog 是交付范围；不能把 MCP-only 写成透明拦截。_
- [x] **t2-plan**: Break work into executable tasks — _确定中英文 token 指南、博客、导航和 release notes 的源文件；`site/` 只由构建生成。_
- [x] **t3-implement**: Implement changes — _已更新中英文 token 指南、博客、博客导航与根/站点 release notes；MCP-only 边界、TTY 门禁、所有权 ledger 和隐私边界均已公开说明。_
- [x] **t4-verify**: Verify with tests/checks — _`git diff --check`、两套 `mkdocs build --strict`、`npm run test:check-site-sync` 与 `npm run test:scripts` 均成功。_

## Verification evidence

- Attach via `aios plan add-evidence --kind command|path|test --value "..."`
- Plan cannot be `done` without evidence and completed tasks
- 2026-07-11: `git diff --check` returned clean.
- 2026-07-11: `python3 -m mkdocs build --strict --config-file mkdocs.yml` and `python3 -m mkdocs build --strict --config-file mkdocs.blog.yml` completed successfully.
- 2026-07-11: `npm run test:check-site-sync` passed 6 tests; `npm run test:scripts` completed successfully.

## Version decision

- Impact: patch-level documentation change.
- No additional version bump: this completes the public explanation for the already-versioned `3.6.0` feature and its release notes. A dry run confirmed that the repository bump helper would create `3.6.1`, but its append-only changelog placement does not match this release-completion scope.

## Status

- status: complete
