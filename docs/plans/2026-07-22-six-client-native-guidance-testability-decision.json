{
  "kind": "behavior-delta",
  "decisionRef": "file:docs/plans/2026-07-22-six-client-native-guidance-testability-decision.json",
  "redCandidate": {
    "publicEntry": "composeNativeMarkdown({ rootDir, client }) for clients sharing the AGENTS.md instruction surface",
    "setup": "Load the real exported native composer from the repository and compose Codex, OpenCode, Hermes, and Grok guidance from the checked-in native sources.",
    "command": {
      "executable": "node",
      "args": [
        "--input-type=module",
        "-e",
        "import assert from 'node:assert/strict'; import { composeNativeMarkdown } from './scripts/lib/native/emitters/compose.mjs'; const clients = ['codex','opencode','hermes','grok']; const outputs = clients.map((client) => composeNativeMarkdown({ rootDir: process.cwd(), client })); assert.equal(new Set(outputs).size, 1, 'shared AGENTS projection must be byte-identical'); assert.doesNotMatch(outputs[0], /AIOS For OpenCode|AIOS Native Hermes|AIOS Native Grok/u);"
      ],
      "cwd": "E:\\coding\\harness-cli"
    },
    "expected": "All four clients that consume AGENTS.md receive one byte-identical client-neutral projection with no OpenCode, Hermes, or Grok operating manual appended.",
    "observed": "The recorded public scenario exited with status 1 because composeNativeMarkdown appends a different client project source for each AGENTS consumer.",
    "failureReason": "The shared composer is capability- and client-dependent, while the Codex emitter additionally concatenates three other client manuals into Codex startup context.",
    "receiptRef": "receipt:8d2a6127-d980-4ec7-80d8-1f8d8c96e96e"
  }
}
